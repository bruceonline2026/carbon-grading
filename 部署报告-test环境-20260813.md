# 企业绿色评级系统 · 测试环境部署报告

> **部署目标**：跳板机 IIS Test 网站（test.tiici.com）
> **部署时间**：2026-08-13 17:38 (GMT+8)
> **执行方式**：SSH 跳板机自动化（paramiko + PowerShell Remoting）
> **脚本**：`tools/deploy_test_env.py` + `tools/ssh_win.py`

---

## ✅ 部署结果总览

| 步骤 | 内容 | 结果 |
|------|------|------|
| 1 | SFTP 上传部署包 192KB → C:\WebServer\Test | ✅ 8 个文件 |
| 2 | 解压（Expand-Archive） | ✅ index.html / web.config / assets/ / join-us/ |
| 3 | config.js 域名更新：__cgUrl__ → https://test.tiici.com | ✅ |
| 4 | win-acme 下载（Let's Encrypt 免费证书） | ❌ 跳板机访问 github.com 受限 |
| 5 | 自签名证书 fallback（PowerShell New-SelfSignedCertificate） | ✅ Thumbprint 19A5701E74E722BBCFF08E4D28398B23DD8FA71A（1 年有效期，含 Server Authentication EKU） |
| 6 | IIS HTTPS 绑定 + netsh sslcert 0.0.0.0:443 + HTTP→HTTPS 301 重定向 | ✅ SNI 模式 SSLFlags=1 |
| 7 | 验证 HTTPS | ⚠️ 服务器侧握手正常（TLS 协议/证书 OK），浏览器会显示"不安全证书"警告（自签名预期行为） |

---

## 📦 部署文件清单

```
C:\WebServer\Test\
├── index.html            814 字节
├── web.config           2272 字节  ← IIS SPA fallback（仅 GET 走 fallback 防 405）
├── assets/
│   ├── config.js         552 字节  ← __cgUrl__ = "https://test.tiici.com"
│   ├── index.css      129742 字节
│   └── index.js       560391 字节
└── join-us/
    └── index.html     16297 字节  ← 顶部 8 项菜单 + 左侧 5 项权益 + 右侧表单
```

---

## 🔐 证书说明（自签名 vs 免费 DV）

**当前部署：自签名证书**（浏览器会显示"您的连接不是私密连接"，需要点"高级→继续访问"）。

**为什么不用 Let's Encrypt 免费证书？**
- win-acme 下载失败：跳板机访问 github.com 受限（连接已关闭/超时）
- ACME HTTP-01 验证可能也不通（test.tiici.com:80 在测试环境内网可能没公网入口）
- 如果生产环境有公网入口，重新执行 `deploy_test_env.py` 的 step4 即可走 Let's Encrypt 自动签发 + 自动续期

**测试环境自签名可接受**（内部团队测试 + 已知来源证书可手动信任），生产部署务必用 Let's Encrypt/云厂商证书。

---

## 🛠 工具脚本（可复用）

| 文件 | 用途 |
|------|------|
| `tools/ssh_win.py` | paramiko SSH 跳板机 + SFTP 上传 + PowerShell 远程执行（含 base64/中文编码处理）|
| `tools/deploy_test_env.py` | 一站式部署：上传→解压→改 config→证书→绑定→验证（7 步可分步执行）|
| `tools/verify_final.ps1` | HTTPS/HTTP/证书/绑定/文件 综合验证 |
| `tools/bind_sni.ps1` | SNI 模式绑定（SSLFlags=1）单独执行 |
| `tools/verify_https.ps1` | HTTPS 探活（含 HTTP 跳转验证）|

**复用命令**：
```bash
python tools/deploy_test_env.py           # 全量重部署
python tools/deploy_test_env.py step1    # 单步重跑（上传+解压）
python tools/deploy_test_env.py step5    # 重做证书
python tools/deploy_test_env.py step6    # 重做绑定
```

---

## ⚠️ 已知遗留

1. **自签名证书**：浏览器/curl 会显示证书不安全警告（生产改 Let's Encrypt 即可解决）
2. **__entUrl__ 仍是 uat-enterprise**：测试环境后端域名 NXDOMAIN（enterprise.tiici.com 不存在），暂保留 UAT 段地址。如有正式测试后端，编辑 `C:\WebServer\Test\assets\config.js` 的 `__entUrl__` 值即可
3. **win-acme 下载失败**：跳板机外网受限（如 ACME 验证不可达，可考虑在公司可访问 ACME 的机器上预生成证书再分发）
4. **PowerShell 自动化探活受 runspace 限制**：服务器侧配置已完整，验证需用户在浏览器/RDP 客户端实际打开

---

## 📋 浏览器验证清单（用户在客户端）

- [ ] 打开 https://test.tiici.com/ → 显示绿色评级系统首页（自签名警告需点"继续"）
- [ ] 首页证书信息：CN=test.tiici.com，颁发给 test.tiici.com，有效期 1 年
- [ ] 访问 http://test.tiici.com/ → 自动 301 跳转到 https
- [ ] 访问 https://test.tiici.com/financial-supermarket → 金融超市页（不再 404）
- [ ] 访问 https://test.tiici.com/certificate-query → 证书查询页（不再 404）
- [ ] 访问 https://test.tiici.com/join-us/ → 加入我们页（8 项菜单 + 左侧权益 + 右侧表单）
- [ ] F12 控制台无 JS 报错

---

*部署时间：2026-08-13 · 工具：`tools/deploy_test_env.py` · 跳板机：47.116.215.156 (nadmin)*