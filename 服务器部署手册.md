# 企业绿色评级系统 · 服务器部署手册

> 版本：v1.1（2026-08-13） · 适用站点：绿色评级系统静态前端（React SPA + join-us 独立页）
> 支持部署方式：**Windows（IIS）** / **Linux（Nginx）** / **Docker（推荐，跨平台一致）**
> 本版本已内置 **HTTPS 证书配置**（三方案均支持）

---

## 一、部署架构总览

```
用户浏览器
    │  HTTPS
    ▼
┌───────────────────────────────────────────────┐
│  站点服务器（本手册部署的对象）                  │
│  · 静态文件：index.html / assets/ / join-us/    │
│  · SPA fallback：子路由刷新回退 index.html      │
│  · 无业务后端，不做 API 代理                    │
└──────────────────────────┬────────────────────┘
                           │ 浏览器直连（跨域 CORS）
                           ▼
             企业后台（uat-enterprise.carbon-grading.com）
             · CertificateLookup（证书查询）
             · FinancialProductLookup（金融产品）
             · FinancialProductTypeList（产品类型）
```

**关键点：** 站点服务器只托管静态文件；所有业务 API 由浏览器直连企业后台，因此**部署本系统无需配置任何后端服务**，只需正确托管静态资源并配置 SPA fallback。

---

## 二、部署包说明

部署包：`deploy-uat-20260811.zip`（约 194KB，7 个文件）

```
部署包解压后：
├── index.html          官网 SPA 入口（React 应用）
├── web.config          IIS 配置（仅 Windows/IIS 使用，Linux/Docker 忽略）
├── assets/
│   ├── config.js       ★ 环境配置（__entUrl__ 企业后台 / __cgUrl__ 官网域名）
│   ├── index.js        压缩后的前端应用（含页面、菜单、接口地址）
│   └── index.css       压缩后的样式
└── join-us/
    └── index.html      「加入我们」独立页面（顶部导航 + 左侧权益 + 右侧申请表）
```

> 发布新版本时，用最新的 zip 覆盖站点目录即可；**备份文件（*.bak/*.orig）不随包发布**。

---

## 三、方案一：Windows + IIS

### 3.1 环境要求

| 项 | 要求 |
|----|------|
| 操作系统 | Windows Server 2016 / 2019 / 2022 |
| Web 服务器 | IIS 8.5+（10.0 最佳） |
| 模块 | **URL Rewrite 模块**（SPA fallback 依赖，IIS 10 通常已装） |

### 3.2 安装 IIS（如未安装）

1. 打开「服务器管理器」→ 添加角色和功能
2. 勾选「Web 服务器 (IIS)」，一路下一步完成安装

### 3.3 检查 URL Rewrite 模块

1. 打开「IIS 管理器」（`inetmgr`）
2. 左侧选中服务器 → 双击「模块」
3. 查找 `UrlRewriteModule`。**若不存在**，下载安装：
   https://www.iis.net/downloads/microsoft/url-rewrite
   （安装后在 IIS 管理器左侧会出现「URL 重写」图标）

### 3.4 部署步骤

```powershell
# 1) 创建网站根目录（示例）
mkdir C:\inetpub\wwwroot\carbon-grading

# 2) 解压部署包到该目录（含 web.config，必须保留！）
#    web.config 内含 SPA fallback 规则 + Cache-Control

# 3) 创建 IIS 网站（或使用默认网站指向该目录）
#    IIS 管理器 → 网站 → 右键「添加网站」
#    网站名称: carbon-grading
#    物理路径: C:\inetpub\wwwroot\carbon-grading
#    端口: 80（或 8080，按需）

# 4) 修改环境配置（见第五章）
#    编辑 C:\inetpub\wwwroot\carbon-grading\assets\config.js
```

### 3.5 web.config 说明（随部署包自带，无需手写）

`web.config` 已内置三条规则：

| 规则 | 作用 |
|------|------|
| 静态资源放行 | `assets/`、`join-us/` 目录及已知文件后缀直接返回 |
| GET SPA fallback | 非真实文件的 **GET** 请求 rewrite 到 `index.html`（解决子路由刷新 404） |
| 非 GET 放行 | POST 等请求不被 fallback 吞掉（避免 API 出现 405） |

> ⚠️ 若部署后访问子路由报 **HTTP 500.19**，说明服务器缺 URL Rewrite 模块，回看 3.3 节安装。

---

## 四、方案二：Linux + Nginx

### 4.1 环境要求

| 项 | 要求 |
|----|------|
| 操作系统 | Ubuntu 20.04+ / CentOS 7+ / Debian 11+ 等 |
| Web 服务器 | Nginx 1.18+ |

### 4.2 安装 Nginx

```bash
# Ubuntu / Debian
sudo apt update && sudo apt install -y nginx

# CentOS / RHEL
sudo yum install -y nginx
```

### 4.3 部署步骤

```bash
# 1) 创建站点目录
sudo mkdir -p /var/www/carbon-grading

# 2) 上传并解压部署包（本机执行）
#    scp deploy-uat-20260811.zip user@server:/tmp/
#    服务器上执行：
cd /var/www/carbon-grading
sudo unzip /tmp/deploy-uat-20260811.zip

# 3) 安装 Nginx 配置（仓库已提供 deploy/nginx.conf，含 SPA fallback + 缓存）
sudo cp /path/to/deploy/nginx.conf /etc/nginx/conf.d/carbon-grading.conf
#    或手动创建（内容见下方 4.4）

# 4) 修改环境配置（见第五章）
sudo vim /var/www/carbon-grading/assets/config.js

# 5) 检查配置并重启 Nginx
sudo nginx -t
sudo systemctl restart nginx
```

### 4.4 手动创建 Nginx 配置（若未使用仓库自带文件）

```nginx
server {
    listen       80;
    server_name  _;                    # 改为实际域名
    root         /var/www/carbon-grading;
    index        index.html;

    # gzip 压缩
    gzip on;
    gzip_min_length 1024;
    gzip_types text/html text/css application/javascript application/json image/svg+xml;

    # 安全头
    add_header X-Content-Type-Options nosniff always;
    add_header X-Frame-Options SAMEORIGIN always;

    # 静态资源长缓存
    location /assets/ {
        expires 365d;
        add_header Cache-Control "public, max-age=31536000, immutable";
        try_files $uri =404;
    }

    # join-us 短缓存
    location /join-us/ {
        expires 1h;
        try_files $uri $uri/ =404;
    }

    # index.html 不缓存
    location = /index.html {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # API 路径放行（本站无后端则返回 404，不被 fallback 吞掉）
    location /DataServices/ {
        return 404;
    }

    # SPA fallback（核心：子路由刷新不 404）
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

> 完整版配置见仓库 `deploy/nginx.conf`（含注释）。

---

## 五、方案三：Docker（推荐）

### 5.1 环境要求

| 项 | 要求 |
|----|------|
| 操作系统 | Linux / Windows（WSL2） / macOS，均可 |
| Docker | Docker Engine 20.10+（含 docker compose 插件） |

### 5.2 镜像构建与启动

仓库已提供 `deploy/Dockerfile` 与 `deploy/docker-compose.yml`。

```bash
# 方式 A：docker compose（推荐）
cd 项目根目录            # 即包含 index.html、deploy/ 的目录
docker compose -f deploy/docker-compose.yml up -d --build

# 方式 B：docker 命令
docker build -t carbon-grading-web:latest -f deploy/Dockerfile .
docker run -d --name carbon-grading -p 8080:80 carbon-grading-web:latest
```

### 5.3 验证

```bash
docker ps                    # 容器运行状态（healthy = 健康）
curl http://127.0.0.1:8080/  # 首页 200
```

### 5.4 更新版本（两种方式）

**方式 A：重建镜像（推荐，一致性最好）**
```bash
# 替换站点文件后
docker compose -f deploy/docker-compose.yml up -d --build
```

**方式 B：挂载静态目录（免重建，改文件立即生效）**
```yaml
# docker-compose.yml 中启用 volume：
    volumes:
      - ./site:/usr/share/nginx/html:ro
# 将站点文件放 ./site/ 目录，替换文件后刷新即可
```

---

## 六、HTTPS 证书配置（三方案通用）

### 6.1 证书获取（先准备证书）

**✅ 推荐方案（默认）：Let's Encrypt 免费证书，一键自动化签发，无需购买**

> **结论：本站无需购买证书。** Let's Encrypt 免费 DV 证书与商业 DV 证书加密强度相同、浏览器全信任，90 天自动续期反而更安全。仅两类场景才需付费：① 地址栏需显示企业名称（EV/OV 证书，金融官网常见）；② 纯内网无法访问 ACME 服务。

仓库已提供**一键自动化签发脚本**（二选一，自动完成：安装工具 → 签发 → 部署到 `/etc/nginx/certs/` → 配置自动续期）：

```bash
# 方式A：certbot 版（推荐，Ubuntu/Debian + Nginx）
bash deploy/scripts/auto-ssl-certbot.sh uat.carbon-grading.com

# 方式B：acme.sh 版（跨发行版；支持 DNS 验证，适合 Docker/内网）
bash deploy/scripts/auto-ssl-acme.sh uat.carbon-grading.com
# DNS 验证示例（无需开放 80 端口）：
#   export CF_Token="你的Cloudflare Token"
#   bash deploy/scripts/auto-ssl-acme.sh uat.carbon-grading.com --dns dns_cf
```

> 前置条件：① 域名已解析到服务器公网 IP；② 80 端口可被外网访问（HTTP-01 验证）；③ 证书输出路径自动对齐 nginx.conf 的 `/etc/nginx/certs/`。

**方式A：商业证书 / 云厂商证书（可选）**
- 阿里云/腾讯云/华为云等购买 SSL 证书，下载 **Nginx 格式**（含 `.crt` 证书 + `.key` 私钥）；部分云厂商提供**免费 DV 证书**（一年期）

**方式B：Let's Encrypt 免费证书（手动 certbot）**
```bash
# 安装 certbot（以 Ubuntu 为例）
sudo apt install -y certbot python3-certbot-nginx
# 为域名签发并自动配置（需域名已解析到服务器）
sudo certbot --nginx -d your-domain.com
```

**方式C：自签名证书（仅测试/内网，浏览器会告警）**
```bash
# 仓库已提供脚本：
bash deploy/scripts/gen-selfsigned-cert.sh ./certs
# 生成 ./certs/server.crt + ./certs/server.key
```

### 6.2 Windows + IIS 证书配置

1. 在服务器上**导入证书**：`mmc` → 文件 → 添加/删除管理单元 → 证书 → 计算机账户 → 个人 → 证书 → 右键"所有任务→导入"，选 `.pfx` 证书（含私钥，需密码）
2. IIS 管理器 → 左侧选中站点 → 右侧「绑定」→ 「添加」：
   - 类型：**https**
   - 端口：**443**
   - 主机名：实际域名
   - SSL 证书：选择刚导入的证书
3. （可选）HTTP→HTTPS 跳转：IIS → 站点 → 「HTTP 重定向」→ 目标 `https://域名/`，勾选"将请求重定向到此目标" + 状态码 301

### 6.3 Linux + Nginx 证书配置

仓库 `deploy/nginx.conf` 已内置 HTTPS（443 + 80→443 跳转），只需放证书：

```bash
# 1) 放置证书（示例：/etc/nginx/certs/）
sudo mkdir -p /etc/nginx/certs
sudo cp server.crt server.key /etc/nginx/certs/

# 2) 修改 nginx.conf 中证书路径（若放默认位置则无需改）
#    ssl_certificate     /etc/nginx/certs/server.crt;
#    ssl_certificate_key /etc/nginx/certs/server.key;

# 3) 检查并重载
sudo nginx -t && sudo systemctl reload nginx
```

**注意：** 证书路径已定义为 `/etc/nginx/certs/`（与 Docker 挂载目录一致），Linux 原生部署放到该目录即可零修改。

### 6.4 Docker 证书配置（推荐）

仓库已内置 HTTPS 配置，只需准备证书并挂载：

```bash
# 1) 生成或放置证书到 ./certs/（与 docker-compose.yml 同级）
mkdir -p certs
#    商业/云证书：复制 server.crt server.key 到 certs/
#    测试自签名：bash deploy/scripts/gen-selfsigned-cert.sh certs

# 2) 启动（compose 已配置 443 端口 + certs 挂载）
docker compose -f deploy/docker-compose.yml up -d --build

# 3) 验证
curl -k https://127.0.0.1/   # 首页 200
docker ps                     # 容器 healthy
```

**证书更新**（证书到期前）：替换 `certs/server.crt` 和 `server.key` → `docker compose -f deploy/docker-compose.yml restart web`，无需重建镜像。

---

## 七、环境配置管理（所有方案通用）

配置文件：`assets/config.js`（部署包内，**每次部署必须核对**）

```js
window.__entUrl__ = "https://uat-enterprise.carbon-grading.com";  // 企业后台基域名
window.__cgUrl__  = "https://uat.carbon-grading.com";            // 官网基域名
```

| 配置项 | 含义 | 影响 |
|--------|------|------|
| `__entUrl__` | 企业后台基域名 | 指标申报/登录外链、CertificateLookup 接口地址 |
| `__cgUrl__` | 官网（本站）基域名 | 顶部菜单 hash 路由跳转（首页/流程/服务/合作伙伴） |

**正式环境切换示例：**
```js
window.__entUrl__ = "https://enterprise.carbon-grading.com";
window.__cgUrl__  = "https://carbon-grading.com";
```

> ⚠️ `assets/index.js` 中金融产品两个接口（FinancialProductLookup / TypeList）的域名需与 `__entUrl__` 指向一致；若环境切换后产品列表为空，检查 index.js 内硬编码域名。

---

## 八、部署后验证清单

| # | 检查项 | 期望 |
|---|--------|------|
| 1 | HTTPS 首页 `https://域名/` | 200，证书正常（无告警），品牌标题渲染 |
| 2 | HTTP 访问 `http://域名/` | 301 跳转到 HTTPS |
| 3 | SPA 子路由 `/financial-supermarket` 直接刷新 | 200（不 404，fallback 生效）|
| 4 | SPA 子路由 `/certificate-query` 直接刷新 | 200 |
| 5 | join-us `/join-us/` | 顶部导航 10 项 + 左侧权益 + 右侧表单 |
| 6 | 菜单跳转 | 首页/流程/服务/合作伙伴/加入我们各路由可达 |
| 7 | 登录/指标申报 | 跳转企业后台 `__entUrl__` |
| 8 | 证书查询（填企业名+编号点查询） | 滑块验证 → 结果卡片正常 |
| 9 | 金融超市 | 产品类型下拉、产品列表正常（接口或兜底）|
| 10 | 控制台（F12）| 无 JS 运行时错误 |
| 11 | 浏览器强刷（Ctrl+F5）| 首页及子路由均正常 |

---

## 九、常见问题排查

| 现象 | 原因 | 解决 |
|------|------|------|
| 子路由刷新 404 | SPA fallback 未配置 | IIS：确认 URL Rewrite + web.config；Nginx：确认 try_files |
| IIS 报 500.19 | 缺 URL Rewrite 模块 | 安装 url_rewrite（见 3.3）|
| 页面白屏 | index.js 语法被破坏 / 缓存 | F12 看报错；强刷；确认 index.js 完整 |
| 金融产品为空 | index.js 内接口域名未切环境 | 核对 index.js 中 FinancialProduct 域名与 `__entUrl__` |
| CORS 报错 | 企业后台未放行跨域 | 联系后台在响应头加 `Access-Control-Allow-Origin: *` |
| 页面内容不更新 | index.html/静态资源被缓存 | 按本手册缓存配置应为 no-cache / 短缓存；手动强刷 |
| Docker 端口占用 | 80/443 被占用 | 改 docker-compose 端口映射 `"8443:443"` 等 |
| HTTPS 证书告警 | 自签名证书 / 域名不匹配 | 换正规证书；确认证书域名与访问域名一致 |
| HTTPS 不生效 | 证书未挂载/路径不对 | Docker：确认 certs/ 挂载；Nginx：nginx -t 检查证书路径 |

---

## 十、回滚方案

| 部署方式 | 回滚操作 |
|----------|----------|
| Windows/IIS | 用上次备份目录覆盖（部署前将旧目录复制为 backup-日期）|
| Linux/Nginx | `sudo cp -r /var/www/carbon-grading.bak/* /var/www/carbon-grading/` |
| Docker | `docker compose up -d --build` 前，先 `docker tag carbon-grading-web:latest carbon-grading-web:backup-日期`，回滚时用旧镜像重启 |

**通用原则：** 每次部署前，对站点目录做一次完整备份（`backup-YYYYMMDD`），回滚即覆盖恢复。

---

## 十一、快速决策表

| 场景 | 推荐方案 |
|------|----------|
| 公司已有 Windows + IIS 运维体系 | 方案一（Windows/IIS）|
| 公有云 Linux 服务器 | 方案二（Linux/Nginx）|
| 新部署 / 多环境 / 想省运维 | **方案三（Docker，推荐）** |

---

*配套文件：`deploy/Dockerfile`、`deploy/docker-compose.yml`、`deploy/nginx.conf`（Linux/Docker 共用）*
*配套文档：`UAT部署说明.md`（RDP 手动部署场景）*
